import java.util.Queue;

class Node {
   int mass, w, h;
   System parent;
   PVector v0, s0, a0, position;
   Queue q;
   
   Node(int mass, int w, int h, System parent) {
     this.mass = mass;
     this.w = w;
     this.h = h;
     this.parent = parent;
   }
   
   void stageChanges(float dx, float distance) {
   
   }
   
   void makeChanges() {
     
   }
   
   void isOver(int mouseX, int mouseY) {
     
   }
   
   void onDrag() {
     
   }
   
   void draw() {
     
   }
}